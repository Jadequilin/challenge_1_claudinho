# Runtime da API — versoes fixas para o ambiente ser identico em todo lugar
# 0.115.x e a linha estavel; a 0.141 traz starlette 1.6, que pede httpx2 no TestClient.
fastapi==0.115.6
uvicorn[standard]==0.34.0
pydantic==2.13.5
pydantic-settings==2.7.0
python-dotenv==1.0.1
supabase==2.31.0
sentence-transformers==6.0.1
# Rate limiting por identidade (Docs/Production/03, secao 2.2)
limits==5.7.0
# Cliente HTTP do LLM (APP/model/llm.py); ja vinha com o supabase, fixado por ser uso direto
httpx==0.28.1
# Validacao do JWT do Supabase Auth (chega junto do supabase, fixado por ser uso direto)
PyJWT==2.13.0
# Treino do classificador de risco (APP/model/train.py). Ja chegavam de carona pelo
# sentence-transformers; fixados aqui por serem uso direto.
scikit-learn==1.9.1
joblib==1.6.0
